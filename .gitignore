node_modules/
.next/
out/
build/

# Secrets — never commit. .env.example is the committed template.
.env
.env.local
.env*.local

# Run output is regenerated by scripts/run.mts and scripts/eval.mts, which both
# create the directory on demand. Nothing in here is a deliverable.
outputs/

.DS_Store
*.log
tsconfig.tsbuildinfo
.next-build
